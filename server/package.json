{
  "name": "whiteboard-server",
  "version": "1.0.0",
  "description": "Express + Socket.IO backend for Real-Time Collaborative Whiteboard",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:client": "cd ../client && npm install && npm run build",
    "start:production": "NODE_ENV=production node index.js",
    "setup": "cd ../client && npm install && cd ../server && npm install && npm run build:client"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
